# 请输入这个程序的功能，重写了父类的方法contextMenuEvent，实现了右键点击事件# pip install -i https://pypi.tuna.tsinghua.edu.cn/simple xxxx # 打包命令 pyinstaller xxxxxxxx --onefile --icon=xxxxxxxxxx #from PyQt5.Qt import *import sysclass window(QWidget):    def contextMenuEvent(self, QContextMenuEvent): #contextMenuEvent是一个事件，右键点击事件,此行代码是重写了父类的方法，所以要加self        print("右键点击了")        menu = QMenu(self)#这一行代码是创建一个菜单        item1 = QAction("菜单项1",menu)#这一行代码是创建一个菜单项        item2 = QAction("菜单项2",menu)        item3 = QAction("菜单项3",menu)        menu.addAction(item1)#这一行代码是添加菜单项，addAction是添加动作，menu.addAction是添加菜单项        menu.addAction(item2)        menu.addAction(item3)        menu.exec_(QContextMenuEvent.globalPos()) #globalPos()是获取鼠标的位置并返回一个QPoint对象，exec_()是显示菜单的方法app = QApplication(sys.argv)window = window()window.show()sys.exit(app.exec_())