# 请输入这个程序的功能 ，重写了QToolButton的setMenu方法，使其可以在菜单显示之前执行一些操作# 在窗口中添加一个工具按钮，点击工具按钮时，显示一个菜单，菜单中有三个菜单项，# pip install -i https://pypi.tuna.tsinghua.edu.cn/simple xxxx # 打包命令 pyinstaller xxxxxxxx --onefile --icon=xxxxxxxxxx #from PyQt5.Qt import *import sysapp = QApplication(sys.argv)window = QWidget()tb=QToolButton(window)tb.setText("工具按钮")tb.move(100,100)#设置图标icon=QIcon("图标头像.ico")tb.setIcon(icon)tb.setIconSize(QSize(50,50))#QSize是一个类，不是函数，功能是设置图标大小，setQSize是一个函数，功能是设置窗口大小tb.setToolTip("这是一个工具按钮")#设置出现的延迟时间tb.setPopupMode(QToolButton.MenuButtonPopup)window.show()sys.exit(app.exec_())