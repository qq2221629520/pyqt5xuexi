# 计时器是mainwindow的一个属性，是一个对象，类型是QTimer# 定时器的信号是timeout，连接的槽函数是displayTime# 定时器的时间间隔是1000毫秒，也就是1秒# 定时器的启动是start，是一个函数# 定时器的停止是stop，是一个函数# 定时器的重启是start，是一个函数# 定时器的暂停是stop，是一个函数# pip install -i https://pypi.tuna.tsinghua.edu.cn/simple xxxx # 打包命令 pyinstaller xxxxxxxx --onefile --icon=xxxxxxxxxx import sysfrom PyQt5.QtCore import QTimerfrom PyQt5.QtWidgets import QApplication, QMainWindowimport timeclass MainWindow(QMainWindow):    def __init__(self):        super().__init__()        self.timer = QTimer()#timer是一个定时器，类型是QTimer，QTimer是类，timer是对象        self.timer.timeout.connect(self.displayTime)#timeout是定时器的信号，connect是连接信号和槽函数        self.timer.start(1000)  # 设置定时器的时间间隔为 1000 毫秒（1 秒）#start是启动定时器，是函数    def displayTime(self):        currentTime = time.strftime("%Y-%m-%d %H:%M:%S", time.localtime())#currentTime是一个字符串，time是模块，strftime是函数，localtime是函数，strftime是将时间转换为字符串，localtime是将时间转换为本地时间        #上一句括号里的意思是：将时间转换为字符串，然后将字符串转换为本地时间        print(currentTime)if __name__ == '__main__':    app = QApplication(sys.argv)    window = MainWindow()    window.show()    sys.exit(app.exec_())